persons: list(include('person'))
---
person: 
    name: str()
    age: int()
    height: num()
    awesome: bool()
    loopback: ip(version=4)
    loopback_subnet_cidr: int(min=0,max=32)
    loopback6: ip(version=6)
    loopback6_subnet_cidr: int(min=0,max=128)
    mac: mac()